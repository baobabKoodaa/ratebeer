<ul>
  <table class="table table-hover">
    <% @ratings.all.order(:created_at).limit(5).each do |rating| %>
        <tr>
          <td> <%= rating %> </td>
          <td><%= link_to rating.user.username, rating.user %>
          </td>
        </tr>
    <% end %>
  </table>
</ul>